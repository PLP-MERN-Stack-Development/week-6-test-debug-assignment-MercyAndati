{
  "name": "mern-testing",
  "private": true,
  "scripts": {
    "install-all": "pnpm --recursive install",
    "test": "jest",
    "test:unit": "jest --selectProjects server --testPathPattern=unit",
    "test:integration": "jest --selectProjects server --testPathPattern=integration",
    "test:e2e": "echo 'Client e2e tests run via Cypress - coming soon!'"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "jest": "^29.0.0"
  }
}
